# If a directory is created, add the line below
# with the same name as the directory you created.
# add_subdirectory(DIRECTORY_NAME)

#add_subdirectory(RiemannTracking)
add_subdirectory(STConverter)
add_subdirectory(GETDecoder)
